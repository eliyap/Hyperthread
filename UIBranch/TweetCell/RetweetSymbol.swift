//
//  RetweetSymbol.swift
//  Pods
//
//  Created by Secret Asian Man Dev on 13/11/21.
//

import UIKit

/// Shell enum for some global variables.
enum RetweetSymbol {
    static let name = "arrow.2.squarepath"
    static let config = UIImage.SymbolConfiguration(weight: .black)
}

enum ReplySymbol {
    static let name = "arrowshape.turn.up.left.fill"
    static let hollowName = "arrowshape.turn.up.left"
    static let config = UIImage.SymbolConfiguration(weight: .regular)
}

enum QuoteSymbol {
    static let name = "quote.bubble.fill"
    static let hollowName = "quote.bubble"
    static let config = UIImage.SymbolConfiguration(weight: .regular)
}
